<!--pages/repair/repair.wxml-->
<!-- 添加报修的按钮 -->
<view class="repair-add" bindtap="handleAddRepair">+报修</view>
<!-- 导入数据处理函数 -->
<wxs src="../wxs/fStr.wxs" module="fStr"> </wxs>

<!-- 引入自定义的tabbar -->
<custom-tabbar list="{{tabs}}" cst-tabbar="cst-tabbar2" act-img="act-img2" act-text="act-text2" bind:tabbarChange="tabbarChange2">

  <!-- 待修理 -->
  <block wx:if="{{tabs[0].isActive}}">
    <view class="repairwait-box" wx:for="{{img_arr0}}" wx:key="index">
      <view class="repairwait-img">
        <image src="{{fStr.formatStr('swipers',item)}}" lazy-load webp></image>
      </view>
      <view class="repairwait-content">
        <view class="house">房间号:东方名都(6#601)</view>
        <view>报修人:蓝灵儿(17878780000)</view>
        <view>报修区域:业主室内</view>
        <view>报修问题:马桶坏了</view>
        <view class="date">提交时间: 2019-02-10, 15:23</view>
      </view>
   </view>
  </block>

  <!-- 修理中 -->
  <block wx:if="{{tabs[1].isActive}}">
    <view class="repairing-box" wx:for="{{img_arr1}}" wx:key="index">
      <view class="repairing-img">
        <image src="{{fStr.formatStr('swipers',item)}}" lazy-load webp></image>
      </view>
      <view class="repairing-content">
        <view class="house">房间号:东方名都(6#601)</view>
        <view>报修人:蓝灵儿(17878780000)</view>
        <view>报修区域:业主室内</view>
        <view>报修问题:马桶坏了</view>
        <view class="date">提交时间: 2019-02-10, 15:23</view>
      </view>
    </view>
  </block>

  <!-- 已修理 -->
  <block wx:if="{{tabs[2].isActive}}">
    <view class="repairfinish-box" wx:for="{{img_arr2}}" wx:key="index">
      <view class="repairfinish-img">
        <image src="{{fStr.formatStr('swipers',item)}}" lazy-load webp></image>
      </view>
      <view class="repairfinish-content">
        <view class="house">房间号:东方名都(6#601)</view>
        <view>报修人:蓝灵儿(17878780000)</view>
        <view>报修区域:业主室内</view>
        <view>报修问题:马桶坏了</view>
        <view class="date">提交时间: 2019-02-10, 15:23</view>
      </view>
    </view>
  </block>
</custom-tabbar>

